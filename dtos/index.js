const { ChannelDto } = require('./channelDto');
const { LeagueDto } = require('./leagueDto');
const { MatchDto } = require('./matchDto');
const { TeamDto } = require('./teamDto');
const { UserDto } = require('./userDto');

module.exports = {
  ChannelDto,
  LeagueDto,
  MatchDto,
  TeamDto,
  UserDto,
};
